var splide = new Splide( '#sliderMarcas', {
    type   : 'loop',
    perPage: 3,
    perMove: 1,
  } );
  
  splide.mount();

  var splide = new Splide( '#sliderProduc', {
    type   : 'loop',
    perPage: 3,
    perMove: 1,
  } );
  
  splide.mount();